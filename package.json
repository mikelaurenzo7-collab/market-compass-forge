{
  "name": "grapevine-intelligence-engine",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "docker-compose up --build",
    "build": "npm run build --workspace=apps/web",
    "seed": "docker compose run --rm web_api python -m web_api.seed",
    "dev:replit": "bash scripts/replit-start.sh"
  }
}
