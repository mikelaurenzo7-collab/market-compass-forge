{
  "name": "grapevine-intelligence-engine",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "docker-compose up --build",
    "build": "npm run build --workspace=apps/web",
    "seed": "docker-compose run --rm api python -m apps.api.seed"
  }
}
