name: GitHub Pages workflow placeholder

on:
  workflow_dispatch:

jobs:
  disabled-for-this-repo:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment path
        run: |
          echo "GitHub Pages deployment is disabled for this monorepo."
          echo "Use Docker/Cloudflare deployment paths documented in docs/deployment.md."
