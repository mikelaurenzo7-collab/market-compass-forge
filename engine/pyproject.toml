[project]
name = "grapevine-engine"
version = "0.1.0"
description = "Pure Python compute library: Monte Carlo simulation, scenarios, graph analytics, deal scoring"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.4.0"]
